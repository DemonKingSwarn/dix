// ah yes, comments

config = {
  hero_data = {
    name = "Astrid";
    max_health = 10;
    max_oxygen = 12.0;

    another_var = config.hero_data.max_health;

    is_diving = true;
  };
};

/* vim: ft=nix */
